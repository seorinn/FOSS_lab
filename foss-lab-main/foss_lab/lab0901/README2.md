# 0901 노트필기 오후
```
중간고사 나옵니다!

컴퓨터 시스템의 네 가지 구성 요소: 사용자, 데이터(정보), HW, SW
HW system의 5개 구성요소: CPU, Memory, Storage Device, Output Devices, Input Devices. 그림 중요 
CPU는 직접적으로 output장치에 내보낼 수 있다 ->  x
```
```
Buffer: 메모리에 내보낼 데이터를 저장>내보내는 코멘드 제어 메모리의 어떤 영역에 메모리를 특별하게 인풋장치에서 쓰는 특별한 메모리는 인풋버퍼
버퍼는 ~를 하는 메모리입니다
위에서 아래로 갈 수록 값은 싸고 용량은 큼 속도는 떨어짐
SW는 크게 두 가지로 나뉨: 1. 시스템 sw – 대표적인 거: 운영체제, 컴파일러/ 2. app // 리눅스는 운영체제
Shell은 리눅스나 운영체제에서 명령어를 입력하기 위한 바깥의 사용자가 인터페이스가 되는 소프트웨어

리눅스 커널 그림 중요
Terminal = 단말기 = 입/출력 장치
Terminal에서 stdio <- stdin, stdout, stderr

Git bash에서 명령어 입력할 때 $는 prompt를 의미, 82102@DESKTOP-4DABQCG는 내 현재 working directory
깃 SC설치하면 아주 작은 리눅스 운영체제가 설치됨: MINGW
깃 배쉬 실행하면 가상의 터미널을 만듬 >> ls -l 명령어를 해석해서 실행해주는 프로그램: shell
우리가 사용하고 있는 shell은 bash bornAgainShell

> tty: 현재 사용하고 있는 터미널, 다른 배쉬 창 켜서 치면 다른 터미널이 뜸
```

